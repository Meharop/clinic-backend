// API Configuration
// In production (Vercel), set REACT_APP_API_URL environment variable
// locally, it falls back to localhost
export const API_URL = process.env.REACT_APP_API_URL || 'http://localhost:5000/api';
